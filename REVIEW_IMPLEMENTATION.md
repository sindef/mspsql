# REVIEW.md implementation tracker

This file tracks implementation of every recommendation in `REVIEW.md`.

| ID | Priority | Affected components | Implementation tasks | Tests | Migration / compatibility | Verification evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | P0 | `internal/control`, `internal/controller`, status conditions | Control result handling now updates only per-site status fields and triggers the leader reconciler; top-level phase/conditions remain leader-derived | Added single-result and interleaved two-control-replica result tests proving aggregate Ready is not promoted; KIND E2E now asserts aggregate Ready never becomes true while the primary site is paused or a major-upgrade phase is active | Existing condition names preserved; no API/schema migration required | `go test ./internal/control` passed; `bash -n test/kind/run.sh` and `shellcheck test/kind/run.sh` passed; full E2E proof pending CI/local KIND | In progress |
| F-002 | P0 | restore/upgrade controllers, operation annotations/status | Added operation finalizer adoption/removal; restore deletion deletes owned isolated targets in every restore phase; upgrade deletion clears preflight, rolls back every safe pre-write major phase, waits during rollback phases, blocks post-write phases with `ForwardRepairRequired`, and supports explicit force-abandon cleanup | Added restore phase table, major-upgrade deletion phase matrix, and force-abandon cleanup tests; still needs restart and E2E delete coverage | Existing terminal upgrades still clear annotations; existing active operations adopt the finalizer on next reconcile | Targeted `go test ./internal/controller -run 'TestRestoreDeletionHandlesEveryPhase|TestRestoreDeletionForceAbandonClearsOperationAnnotations|TestMajorUpgradeDeletionPhaseMatrix|TestMajorUpgradeDeletionForceAbandonClearsAnnotations'` passed | In progress |
| F-003 | P0 | `SiteRegistration` controller/webhook, WireGuard | Added site finalizer adoption; direct deletion blocks while referenced unless instances are force-orphaned; unreferenced deletion reuses revocation teardown for token, peer, allocation, and rendered peers; rendered peers are hashed into the gateway Deployment template and deletion waits for gateway observation/availability before finalization; manager RBAC now grants Deployment get/list/watch/update/patch for gateway observation and peer-hash rollout | Added direct deletion block, revoke, gateway-observation, and regenerated manager RBAC coverage; still needs old-key traffic failure E2E | Existing sites adopt the finalizer on next reconcile; revoke behavior remains compatible; clusters without the packaged gateway Deployment keep existing behavior | Targeted `go test ./internal/controller -run ...SiteRegistration...`, `go test ./internal/wireguard`, `make manifests`, and `make verify-manifests` passed | In progress |
| F-004 | P0 | agent RBAC, registration bootstrap, admission, tests | Added regression coverage that rendered workloads explicitly deny privilege escalation/privileged mode; generated bootstrap RBAC and checked-in agent RBAC now scope Secret get/delete/update to mspsql-owned Secret names plus agent bootstrap/identity/Vault CA material instead of broad Secret access and include NetworkPolicy mutation for rendered workload policies | Renderer privilege test added; generated RBAC test proves broad `get/delete secrets` is denied while owned Secret names, agent self-secrets, and NetworkPolicy patch/delete remain allowed; E2E upgrade now reapplies the current generated registration bundle before rolling site-agent images and asserts live `can-i` denies unrelated tenant Secret get/delete while allowing owned Secret get/update/delete and NetworkPolicy operations; still needs two-stage namespace RBAC split and owned reconcile/delete E2E | Bootstrap RBAC derives member certificate Secret names from the registered site name and includes fixed agent bootstrap/identity/Vault CA names required after bundle reapply; bound sites can reapply the short-lived generated bundle to refresh RBAC without changing `SiteRegistration` UID, and existing identities skip rebinding while scrubbing bootstrap tokens | Targeted renderer test passed; `go test ./internal/registration`, `make verify-manifests`, `bash -n test/kind/run.sh`, and `shellcheck test/kind/run.sh` passed | In progress |
| F-005 | P0 | control client, directives, operation state | Receive loop now runs directives in per-operation workers, keeps consuming plans/certificates/cancels while directives execute, cancels matching directive workers, persists directive Running/terminal state in site-agent ConfigMaps, replays terminal results without re-execution after reconnect, resumes idempotent running operations, and rejects empty unsupported messages | Added signed directive cancellation regression, persisted terminal replay regression, running-operation reconnect regression, and race coverage for the control client; still needs live hung backup/cert-renewal timing proof | Wire protocol preserved; `CancelOperation` now has active client semantics; directive state ConfigMaps are additive in the agent namespace and use existing agent ConfigMap RBAC | `go test ./internal/control`, `go test -race ./internal/control`, and `go test ./cmd/site-agent` passed | In progress |
| F-006 | P0 | major upgrade controller/agent/tests/docs | Reordered major upgrades so `MajorReplicas` runs with Pgpool drained; `MajorRestoreWrites` starts Pgpool only after topology and synchronous replication are verified; write acceptance no longer sets `synchronous_commit=local`; README updated | Added agent workload/acceptance regressions and controller sync-gate regression; still needs primary-kill E2E around write restoration | Existing phase names preserved, but `MajorRestoreWrites` now occurs after replica reseed | Targeted `go test ./internal/agent` and `go test ./internal/controller -run ...MajorUpgrade...` passed | In progress |
| F-007 | P0 | backup controller/agent/status/docs | Added restore-drill evidence fields; backup results now keep `RecoveryWindowAvailable=False` until WAL continuity is paired with disposable restore proof; restore preflight rejects metadata-only windows; annotated disposable restore drills can run from BackupReady/WAL metadata and stamp source `restoreDrillLastVerifiedAt`/`restoreDrillBackupSet` on completion | Added restore preflight regression, disposable-restore evidence regression, and KIND PITR drill assertions that the source recovery window becomes drill-verified after the isolated restore validates data; still needs corrupt WAL/object test, sampled periodic controller cadence, and RPO/RTO evidence | Status fields are additive and generated into CRD/deepcopy; the `multisite-postgres.dev/restore-drill=true` annotation is additive and only relaxes preflight for isolated drill restores | Focused control/controller tests, `go test ./internal/controller -run 'TestRestorePreflightRequiresDisposableRestoreEvidence|TestRestoreDrillCanCreateAndRecordDisposableEvidence|TestRestoreCreatesIsolatedTargetAndAdvancesAfterPromotion'`, `bash -n test/kind/run.sh`, and `shellcheck test/kind/run.sh` passed; full E2E proof pending CI/local KIND | In progress |
| F-008 | P0 | registration, identity storage | Added deterministic hashed cluster UID claim ConfigMap before certificate/peer issuance; winner retries are idempotent; failed pre-commit credential paths release a newly created claim | Added binding claim, duplicate registration conflict, and 100-way concurrent claim tests; still needs true multi-replica HTTP stress in E2E/CI | Existing bound sites can recreate the owner-matching claim; claim stores only hash plus owner identity | `go test ./internal/registration` passed | In progress |
| F-009 | P0 | render TLS/SANs, NetworkPolicy, services | Removed etcd `--peer-skip-client-san-verification`; peer client certificate auth remains enabled; site agent now renders ingress NetworkPolicies for etcd, postgres/patroni/pgBackRest, and pgpool, with etcd/postgres access limited to same-instance pods; etcd peer and client certificates can now use separate issuer refs while preserving `etcdIssuerRef` fallback | Added renderer regression for strict peer args, workload NetworkPolicy selectors/ports, and split etcd peer/client issuer selection; added policy validation regression for split etcd issuers; existing readiness regression rejects wrong SANs; still needs live wrong-member certificate and NetworkPolicy reachability E2E | Existing cert Secret names preserved; new `etcdPeerIssuerRef`/`etcdClientIssuerRef` fields are additive and default to `etcdIssuerRef` behavior when omitted | Targeted renderer/policy tests passed; `go test ./api/v1alpha1 ./internal/agent ./internal/controller ./internal/webhook/v1alpha1` passed in focused runs; generated CRD/deepcopy updated | In progress |
| F-010 | P1 | policy/status/controller | Data-site disconnects still fail closed; witness/control-only disconnects now publish `ControlPlaneDegraded` and allow safe data-site reconciliation; restore/upgrade topology-changing plans are blocked while observer/control-only sites are disconnected; backup scheduling uses data-site convergence/topology instead of aggregate `Ready`; operations docs describe degradation decisions | Added backup-scheduling decision table for witness outage, primary/standby data-site staleness, topology/TLS gaps, and active restore/upgrade/child operations; added topology-changing operation decision table and reconcile regression proving witness disconnect blocks signed upgrade plan issuance | No API schema change; condition names additive (`TopologyAuthoritative`) | `GOCACHE=/tmp/mspsql-go-cache go test ./internal/controller -run 'TestTopologyChangingOperationDecisionTable|TestWitnessDisconnectBlocksTopologyChangingPlan|TestBackupSchedulingUsesDataSiteReadinessDuringControlDegradation'` passed; still needs hub-loss E2E | In progress |
| F-011 | P1 | agent apply/ownership/docs | Removed blanket `ForceOwnership` from server-side apply, added a distinct `ErrFieldOwnershipConflict` wrapper for API conflicts, and documented operator repair behavior | Added apply-option regression, representative Service/cert-manager Certificate/StatefulSet conflict wrapping tests, and a live envtest API-server managed-fields conflict proof showing an externally owned Service selector is preserved | Existing field ownership is no longer forcibly taken on reconcile | Targeted `go test ./internal/agent -run 'TestApplyResolvesTypedObjectGVK|TestApplyReportsFieldOwnershipConflict|TestNamespaceOwnershipIsNotAdopted'` passed; `KUBEBUILDER_ASSETS=/home/mnorris/repos/mspsql/bin/k8s/1.36.2-linux-amd64 GOCACHE=/tmp/mspsql-go-cache go test ./internal/agent -run TestApplyReportsLiveAPIServerFieldOwnershipConflict` passed | In progress |
| F-012 | P1 | control delivery/indexes/agent queues/benchmarks | Control status updates now suppress unchanged per-site writes; site-agent apply locking is per instance UID instead of one global site-wide mutex; the hub manager registers a `MultiSitePostgres` UID field index and control status/trigger paths use indexed lookup with fallback for non-cache clients; directive delivery now applies a bounded per-sync cap so a single stream cannot drain unbounded queued work in one pass | Added unchanged-write regression, indexed 1,000-instance/100-site unchanged-write benchmark, directive backpressure regression, 10,000-directive replay/selection benchmark with p99 per-directive latency metric, and race-covered per-instance lock test; still needs informer queues and live plan-delivery measurement | No API/schema change | `go test ./cmd ./internal/control` passed; `go test -race ./internal/agent -run TestReconcilerLocksPerInstance` passed; `go test -run '^$' -bench BenchmarkUpdateInstanceSiteSkipsUnchangedStatusWriteAtFleetScale -benchtime=10x ./internal/control` passed at 134.6 ms/op with zero status writes; `GOCACHE=/tmp/mspsql-go-cache go test ./internal/control` passed; `GOCACHE=/tmp/mspsql-go-cache go test -run '^$' -bench BenchmarkSendDirectivesAtFleetScale -benchtime=5x ./internal/control` passed at 309.7 ms/op with 10,000 queued directives, 100 directives sent for one site stream, and 3,133,544 p99_ns_per_directive | In progress |
| F-013 | P1 | registration CA, signing keys, WireGuard | New plan-signing keys, WireGuard hub identities, WireGuard peers, and bind responses now carry stable key IDs plus revocation epoch; bootstrap persists issued key IDs for new agents; design docs describe overlapping-trust rotation order | Added signing-key metadata, bind-response metadata, WireGuard identity/peer metadata tests; still needs full dual-trust roots, per-site rolling WireGuard key/address rotation, old-credential rejection, and interruption recovery E2E | Metadata is additive; existing immutable Secrets without key IDs remain readable | Targeted controller, registration, wireguard, and site-agent tests passed | In progress |
| F-014 | P1 | parent/child finalizers/admission/docs | Database/user deletion now detects missing parent instances, publishes actionable `ParentUnavailable`, supports explicit `force-orphan` finalizer removal with residual cleanup warning, parent instance deletion blocks while child declarations still reference it, and docs describe child-before-parent deletion | Added parent-missing database block, database child-first cleanup directive, terminal user finalizer release, user force-orphan, and parent-child deletion block tests; still needs disconnected site and missing-registration E2E | Reuses existing `multisite-postgres.dev/force-orphan` annotation for compatibility with instance deletion; parent force-orphan remains the explicit bypass for residual child resources | Targeted `go test ./internal/controller -run 'TestDatabaseDeletionBlocksWhenParentMissing|TestDatabaseDeletionWithParentIssuesCleanupDirective|TestUserDeletionForceOrphansWhenParentMissing|TestUserTerminalDeletionReleasesFinalizer|TestInstanceDeletionBlocksWhileChildDeclarationsExist'` passed | In progress |
| F-015 | P1 | protocol, plan schema, capability gates/docs | Added required capability gate for major upgrades; signed plans now include additive `requiredCapabilities`; current site agents advertise `major-upgrade-sync-before-writes`; blocked status names the site/registration/capability; install docs publish N/N-1 order and downgrade limits | Added capability-gate unit test, required-capability helper assertions, and unknown optional-field plan verification test; still needs reconnect and rollback E2E compatibility proof | Protocol string unchanged; capability gate prevents unsafe rollout to older agents; `requiredCapabilities` is additive and ignored by older agents | Targeted controller/plan capability tests and `go test ./cmd/site-agent` passed | In progress |
| F-016 | P1 | Dockerfiles, release workflow, admission/docs | Admission now rejects mutable `MultiSitePostgres.spec.postgres.image`, `PostgresUpgrade.spec.targetImage`, and `upgradeImage`; samples, docs examples, and KIND instance use the real Percona multi-arch digest; production Dockerfile bases are pinned by digest; release builds publish amd64/arm64 SBOM/provenance, scan pushed digests for unapproved High/Critical CVEs, and sign immutable digests; CI verifies image policy, scans Go source/dependencies with pinned govulncheck, and scans every locally built runtime image; the upgrade image applies vendor package security updates during build | Added webhook rejection tests and `hack/verify-image-policy.sh`; CI/release Trivy scans and govulncheck source scan added; still needs final release artifact proof from an actual tag | Existing mutable CRs require image update before mutation; released `latest` tag removed from workflow; example/base digests resolved from `docker buildx imagetools inspect`; Trivy action pinned to the full v0.36.0 commit SHA; govulncheck pinned to `golang.org/x/vuln/cmd/govulncheck@v1.6.0` | `go test ./internal/webhook/v1alpha1`, `make verify-image-policy`, `shellcheck hack/verify-image-policy.sh`, dependency-bump `make lint`, `make test`, `make verify-generated`, `make test-race`, and `make govulncheck` passed | In progress |
| F-017 | P1 | operation records/retries/metrics/runbooks | Added bounded post-upgrade backup retry cap with `ManualInterventionRequired`; added additive shared `status.operation` progress record fields and wired restore progress, declaration directive progress, scheduled backup progress, instance deletion progress, and post-upgrade backup retries to durable operation UID, attempt, site, last error, deadline, terminal state, and bounded jittered `nextRetryAt` where retries apply; hub telemetry now exports operation attempt/deadline/manual-repair metrics and optional Prometheus alerts cover heartbeat, plan lag, sync-write loss, stale backup evidence, overdue deadlines, and manual repair; added diagnostics bundle capture script for hub/site objects, events, operation Jobs, and logs without Secret data | Added retry-limit, retry-wait, restore operation-progress, upgrade operation-progress, declaration operation-result, scheduled-backup operation-result, instance-deletion operation, live deletion-finalizer operation assertion in KIND, operation-metric regressions, and shell syntax/ShellCheck coverage for diagnostics capture; still needs game day execution | Status schema change is additive; legacy `postUpgradeBackupAttempt`/`postUpgradeBackupRequestedAt` remain populated for compatibility; instance/declaration/backup-schedule operation fields are additive; prometheus overlay is optional; diagnostics script is optional operational tooling | Targeted `go test ./api/v1alpha1 ./internal/controller ./internal/control` passed; `make test`, `make lint`, `make test-race`, `make verify-image-policy`, and `make govulncheck` passed | In progress |
| F-018 | P2 | API versions/conversion/docs | Added explicit v1alpha1 storage compatibility constants and documented the storage/downgrade window; new operation status fields and split etcd issuer refs are additive on existing v1alpha1 objects | Added broad v1alpha1 CR JSON round-trip tests covering workload, registration, restore, upgrade, database, user, operation-progress, backup schedules, split etcd issuers, and condition fields; still needs Kubebuilder-scaffolded next API version, conversion webhook, storage-version migration, and downgrade E2E | Compatibility policy is additive and preserves current storage version; CRDs/deepcopy regenerated for added status fields and issuer refs | `go test ./api/v1alpha1` and `make verify-generated` passed after generation | In progress |
| S-001 | Security addendum | site-agent plan cache | Equal-revision signed plan replays are now idempotent only when the cached plan bytes and signature match exactly; changed contents or signatures at the same revision are rejected instead of replacing cache state | Extended cache rollback regression to cover exact duplicate replay plus same-revision equivocation | No protocol/schema change; valid monotonic revisions continue to replace older cache entries | Targeted `go test ./internal/agent -run TestCacheRejectsRollback` passed | In progress |
| P3 | P3 | hub services/architecture docs | Sharding remains deferred pending measurements; added first local fleet-scale unchanged-status benchmark evidence | Still needs full scale harness for plan delivery, API QPS, stream reconnect rate, memory, directive queue age, and scrape duration before any service split | Preserve single logical source of truth | Benchmark evidence recorded under F-012 | In progress |
