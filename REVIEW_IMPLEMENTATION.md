# REVIEW.md implementation tracker

This file tracks implementation of every recommendation in `REVIEW.md`.

| ID | Priority | Affected components | Implementation tasks | Tests | Migration / compatibility | Verification evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | P0 | `internal/control`, `internal/controller`, status conditions | Control result handling now updates only per-site status fields and triggers the leader reconciler; top-level phase/conditions remain leader-derived | Added `TestPlanResultDoesNotSetAggregateReady`; still needs race and E2E unsafe-transition watch | Existing condition names preserved; no API/schema migration required | `go test ./internal/control` passed | In progress |
| F-002 | P0 | restore/upgrade controllers, operation annotations/status | Added operation finalizer adoption/removal; restore deletion deletes owned isolated targets; upgrade deletion clears preflight, rolls back safe pre-write major phases, blocks post-write phases with `ForwardRepairRequired`, and supports explicit force-abandon cleanup | Added targeted restore/upgrade deletion tests; still needs exhaustive per-phase, restart, and E2E delete coverage | Existing terminal upgrades still clear annotations; existing active operations adopt the finalizer on next reconcile | Targeted `go test ./internal/controller -run ...` passed | In progress |
| F-003 | P0 | `SiteRegistration` controller/webhook, WireGuard | Added site finalizer adoption; direct deletion blocks while referenced unless instances are force-orphaned; unreferenced deletion reuses revocation teardown for token, peer, allocation, and rendered peers | Added direct deletion block and revoke tests; still needs gateway observation/old-key E2E | Existing sites adopt the finalizer on next reconcile; revoke behavior remains compatible | Targeted `go test ./internal/controller -run ...SiteRegistration...` passed | In progress |
| F-004 | P0 | agent RBAC, registration bootstrap, admission, tests | Added regression coverage that rendered workloads explicitly deny privilege escalation/privileged mode | Renderer privilege test added; still needs two-stage namespace RBAC split, unrelated-secret `can-i` negative tests, owned reconcile/delete tests | No manifest/RBAC change in this slice | Targeted renderer test passed | In progress |
| F-005 | P0 | control client, directives, operation state | Receive loop now runs directives in per-operation workers, keeps consuming plans/certificates/cancels while directives execute, cancels matching directive workers, and rejects empty unsupported messages | Control package compiles/tests; still needs hung backup/cert-renewal timing, persisted dedupe, reconnect, and race tests | Wire protocol preserved; `CancelOperation` now has active client semantics | `go test ./internal/control` passed | In progress |
| F-006 | P0 | major upgrade controller/agent/tests/docs | Reordered major upgrades so `MajorReplicas` runs with Pgpool drained; `MajorRestoreWrites` starts Pgpool only after topology and synchronous replication are verified; write acceptance no longer sets `synchronous_commit=local`; README updated | Added agent workload/acceptance regressions and controller sync-gate regression; still needs primary-kill E2E around write restoration | Existing phase names preserved, but `MajorRestoreWrites` now occurs after replica reseed | Targeted `go test ./internal/agent` and `go test ./internal/controller -run ...MajorUpgrade...` passed | In progress |
| F-007 | P0 | backup controller/agent/status/docs | Added restore-drill evidence fields; backup results now keep `RecoveryWindowAvailable=False` until WAL continuity is paired with disposable restore proof; restore preflight rejects metadata-only windows | Added restore preflight regression; still needs actual disposable-restore controller, corrupt WAL/object test, sampled PITR drill, RPO/RTO evidence | Status fields are additive and generated into CRD/deepcopy | Focused control/controller tests and `make verify-manifests` passed; `verify-generated` generator ran and reported expected uncommitted generated diff before commit | In progress |
| F-008 | P0 | registration, identity storage | Added deterministic hashed cluster UID claim ConfigMap before certificate/peer issuance; winner retries are idempotent; failed pre-commit credential paths release a newly created claim | Added binding claim, duplicate registration conflict, and 100-way concurrent claim tests; still needs true multi-replica HTTP stress in E2E/CI | Existing bound sites can recreate the owner-matching claim; claim stores only hash plus owner identity | `go test ./internal/registration` passed | In progress |
| F-009 | P0 | render TLS/SANs, NetworkPolicy, services | Removed etcd `--peer-skip-client-san-verification`; peer client certificate auth remains enabled | Added renderer regression for strict peer args; still needs separate peer/client CAs, wrong-member cert tests, NetworkPolicies, and reachability E2E | Existing cert Secret names preserved | Targeted renderer test passed | In progress |
| F-010 | P1 | policy/status/controller | Data-site disconnects still fail closed; witness/control-only disconnects now publish `ControlPlaneDegraded` and allow safe data-site reconciliation; backup scheduling uses data-site convergence/topology instead of aggregate `Ready` | Added safe-backup-during-control-degradation predicate test; still needs full operation decision table for witness/standby/primary/hub loss | No API schema change; condition names additive | Targeted controller policy test passed | In progress |
| F-011 | P1 | agent apply/ownership/docs | Removed blanket `ForceOwnership` from server-side apply and added a distinct `ErrFieldOwnershipConflict` wrapper for API conflicts | Added apply-option regression; still needs representative Service/Certificate/StatefulSet managed-field conflict tests and docs | Existing field ownership is no longer forcibly taken on reconcile | Targeted `go test ./internal/agent -run TestApplyResolvesTypedObjectGVK` passed | In progress |
| F-012 | P1 | control delivery/indexes/agent queues/benchmarks | Control status updates now suppress unchanged per-site writes | Added unchanged-write regression; still needs informer queues, indexes, per-instance queues, backpressure, and fleet benchmark | No API/schema change | `go test ./internal/control` passed | In progress |
| F-013 | P1 | registration CA, signing keys, WireGuard | Versioned key IDs; dual trust; per-site WG rotation; revocation epoch | Four-cluster independent rotations and interruption recovery | Temporary dual trust with deadlines | Pending | Not started |
| F-014 | P1 | parent/child finalizers/admission/docs | Database/user deletion now detects missing parent instances, publishes actionable `ParentUnavailable`, and supports explicit `force-orphan` finalizer removal with residual cleanup warning | Added parent-missing database block and user force-orphan tests; still needs parent ordering, disconnected site, missing registration, and docs | Reuses existing `multisite-postgres.dev/force-orphan` annotation for compatibility with instance deletion | Targeted `go test ./internal/controller -run ...ParentMissing` passed | In progress |
| F-015 | P1 | protocol, plan schema, capability gates/docs | Added required capability gate for major upgrades and current site agents advertise `major-upgrade-sync-before-writes`; blocked status names the site/registration/capability | Added capability-gate unit test; still needs N/N-1 matrix, optional/required unknown-field tests, reconnect and rollback compatibility docs | Protocol string unchanged; capability gate prevents unsafe rollout to older agents | Targeted controller capability test and `go test ./cmd/site-agent` passed | In progress |
| F-016 | P1 | Dockerfiles, release workflow, admission/docs | Admission now rejects mutable `MultiSitePostgres.spec.postgres.image` references; samples, docs examples, and KIND instance use the real Percona multi-arch digest | Added webhook rejection test; still needs Dockerfile base pinning, multi-arch SBOM/provenance/signing/scans, release policy, and operator image digest enforcement | Existing mutable CRs require image update before mutation; example digest resolved from `docker buildx imagetools inspect` | `go test ./internal/webhook/v1alpha1` passed | In progress |
| F-017 | P1 | operation records/retries/metrics/runbooks | Added bounded post-upgrade backup retry cap with `ManualInterventionRequired` terminal repair signal instead of unbounded directive churn | Added retry-limit regression; still needs shared operation record, jitter/deadlines, metrics/alerts, diagnostics, and runbooks/game day | Existing retry field reused; no schema change | Targeted post-upgrade backup retry tests passed | In progress |
| F-018 | P2 | API versions/conversion/docs | Define compatibility; add next version/conversion/migration tests | Round-trip/storage migration/downgrade tests | Publish downgrade limits | Pending | Not started |
| P3 | P3 | hub services/architecture docs | Sharding/service split only after measurement requires it | Scale evidence | Preserve single logical source of truth | Pending | Not started |
